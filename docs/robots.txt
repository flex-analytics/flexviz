# mkdocs generates sitemap.xml from `site_url`; this line is how crawlers find it.
# Cloudflare prepends its managed content-signals block to whatever we serve here,
# so the two `User-agent: *` groups merge -- same as flexviz.tech/robots.txt.
User-agent: *
Allow: /

Sitemap: https://docs.flexviz.tech/sitemap.xml
